/* const numbers = [19, 21, 30, 3, 45, 22, 15];

const findDivisibleBy3And5 = () => {
  const isDivisible = (number) => number % 3 === 0 && number % 5 === 0 ;  
  const numbers1 = numbers.find(isDivisible)
  return numbers1
}

console.log(findDivisibleBy3And5()) */
/* const listNames = ['Maria', 'Manuela', 'Jorge', 'Ricardo', 'Wilson'];

const verifyFirstLetter = (letter, names) => names.some((name) => name[0] === letter);

console.log(verifyFirstLetter('J', listNames)); // true
console.log(verifyFirstLetter('x', listNames)); // false */
/* const names = ['Mateus', 'José', 'Ana', 'Cláudia', 'Bruna'];

const hasName = (arr, name) => {
  return names.some((element) => name === element)
}

console.log(hasName(names, 'kinterKelly')) */
/* const people = [
  { name: 'Mateus', age: 18 },
  { name: 'José', age: 18 },
  { name: 'Ana', age: 23 },
  { name: 'Cláudia', age: 20 },
  { name: 'Bruna', age: 18 },
];

const verifyAges = (arr, minimumAge) => {
return people.every((idade) => idade.age >= minimumAge )
}

console.log(verifyAges(people, 18)); */